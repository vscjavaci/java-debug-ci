import path from 'path'
export const ROOT = path.normalize(path.join(__dirname, '../../testcase'));
export const LANGUAGE_SERVER_ROOT = path.normalize(path.join(__dirname, '../../../jdtls'));
export const LANGUAGE_SERVER_WORKSPACE = path.normalize(path.join(__dirname, '../../ws'));